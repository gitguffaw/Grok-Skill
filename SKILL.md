---
name: Grok
description: Deprecated. Use the grok-router plugin from Claude Code or Codex. Do not load this skill inside Grok TUI.
---

# Grok (legacy)

This repo is Grok Router. Install the plugin in Claude Code or Codex. Do not clone this tree onto `~/.grok/skills/Grok`.

```
/plugin marketplace add gitguffaw/Grok-Skill
/plugin install grok-router@grok-router
```

If this file is still sitting in `~/.claude/skills/Grok` or `~/.codex/skills/Grok`, delete that path after the plugin is installed.
